# Pipeline dependencies — PINNED to the exact versions the shipped UI and
# pipeline were verified with, so every clone runs with the same behavior.
# (Verified 2026-07: full test suite + browser e2e on this exact set.)
anthropic==0.116.0
# MTI_Codex branch: OpenAI provider (AI_PROVIDER=openai). Not imported unless
# that env var is set, so this stays an optional, always-installed dependency.
openai==2.46.0
pywin32==312; platform_system == "Windows"
pillow==12.3.0
pdf2image==1.17.0
PyMuPDF==1.28.0
ezdxf==1.4.4
ezdwg==0.9.0
opencv-python-headless==5.0.0.93
# numpy is capped below 2.5: numba (pulled in by cadquery below) requires
# numpy<2.5, and opencv above requires numpy>=2 -- 2.4.6 satisfies both. Bumping
# to 2.5.x makes the cadquery/numba install fail to resolve.
numpy==2.4.6
pydantic==2.13.4
python-dotenv==1.2.2
rich==15.0.0
pytest==9.1.1
# Must-meet verification layer: CadQuery pre-validation (headless geometry
# check before SolidWorks) + trimesh post-build STL verification.
cadquery==2.8.0
trimesh==4.12.2
# scipy is REQUIRED by the post-build STL verification (constraint_verify.py):
# trimesh's mesh.section(...).discrete pulls it in lazily. trimesh's base wheel
# does NOT install scipy (it's an optional trimesh extra), so it must be pinned
# explicitly here -- otherwise post-build must-meet verification dies with
# "No module named 'scipy'" whenever cadquery (which also happens to pull scipy)
# is absent.
scipy==1.18.0
networkx==3.6.1
shapely==2.1.2
