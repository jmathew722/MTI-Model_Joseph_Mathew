# Web UI deps only — the pipeline's own deps come from ../requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
# DWG/DXF intake: DXF files are rendered directly; DWG additionally requires the
# free ODA File Converter (https://www.opendesign.com/guestfiles/oda_file_converter)
# installed on this machine — the server finds it automatically.
ezdxf>=1.3
matplotlib>=3.8
